import {NextRequest,NextResponse} from "next/server"; import {z} from "zod"; import {validate} from "@/lib/licenses"; const schema=z.object({licenseKey:z.string().regex(/^EQTY-[A-Z2-9]{4}-[A-Z2-9]{4}-[A-Z2-9]{4}-[A-Z2-9]{4}$/i),deviceId:z.string().min(16).max(128)}); export async function POST(req:NextRequest){const p=schema.safeParse(await req.json().catch(()=>null));if(!p.success)return NextResponse.json({valid:false,reason:"invalid_request"},{status:400});const result=await validate(p.data.licenseKey,p.data.deviceId);return NextResponse.json(result,{status:result.valid?200:403,headers:{"Cache-Control":"no-store"}})}
