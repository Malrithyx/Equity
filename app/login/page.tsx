"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; export default function Login(){const r=useRouter(),[u,setU]=useState("Equity"),[p,setP]=useState(""),[e,setE]=useState(""),[busy,setBusy]=useState(false);async function go(x:React.FormEvent){x.preventDefault();setE("");setBusy(true);try{const q=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:u,password:p})});const raw=await q.text();let d:{error?:string}={};try{d=raw?JSON.parse(raw):{}}catch{}if(!q.ok){setE(d.error||`Login failed (HTTP ${q.status})`);return}r.replace("/dashboard")}catch{setE("Could not reach the login service.")}finally{setBusy(false)}}return <main className="login"><section><div className="logo">E</div><small>PRIVATE WORKSPACE</small><h1>Equity Control</h1><p>Issue and manage timed access keys for your panel.</p><form onSubmit={go}><label>Username<input value={u} onChange={x=>setU(x.target.value)}/></label><label>Password<input type="password" value={p} onChange={x=>setP(x.target.value)} autoFocus/></label>{e&&<div className="error" role="alert">{e}</div>}<button disabled={busy}>{busy?"Signing in…":"Enter dashboard"}</button></form></section></main>}
